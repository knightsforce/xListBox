“ака€ мешанина и парсингом из-за применени€ JSON.

ѕри изменени€х пересоздаю список, при элементах = 5000 уходит меньше половины секунды.

closest вместо hasClass, если target вложенный узел, например нужен div, а внутри него span, при нажатии на span target = span, closest позвол€те получить нужного родител€ на лбой вложенности. ѕроверка на длину, нужна потому что если нужного родител€ нет, то будет пустой объект, который при преобразованию к boolean = true

ћог бы обойтись без класса active, а использовать в CSS 3 [selected=true],
но вам нужны пользователи со старыми браузерами 

ћогу переписать без делегировани€, но обработчики занимают пам€ть. Ћучше 1 обработчик с if else, чем 100.